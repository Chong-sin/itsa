#include <iostream>
using namespace std;

#define r 200

int main() {
    int x, y;
    cin >> x >> y;
    if (x * x + y * y > r * r)
        cout << "outside" << endl;
    else
        cout << "inside" << endl;
    return 0;
}
